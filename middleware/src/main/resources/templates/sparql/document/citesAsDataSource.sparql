PREFIX cito: <http://purl.org/spar/cito/>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

CONSTRUCT {
    ?dataSource rdfs:label ?label .
} WHERE {
    <[(${uri})]> cito:citesAsDataSource ?dataSource .
    ?dataSource rdfs:label ?label .
}