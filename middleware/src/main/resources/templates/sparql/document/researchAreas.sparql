PREFIX core: <http://vivoweb.org/ontology/core#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

CONSTRUCT {
    ?researchArea rdfs:label ?label .
} WHERE {
    <[(${uri})]> core:hasResearchArea ?researchArea .
    ?researchArea rdfs:label ?label .
}
