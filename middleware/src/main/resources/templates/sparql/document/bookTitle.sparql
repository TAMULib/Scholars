PREFIX tamu: <http://vivo.library.tamu.edu/ontology/TAMU#>

CONSTRUCT {
    <[(${uri})]> tamu:bookTitleForChapter ?bookTitleForChapter .
} WHERE {
    <[(${uri})]> tamu:bookTitleForChapter ?bookTitleForChapter .
}