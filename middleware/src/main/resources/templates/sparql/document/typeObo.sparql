PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

CONSTRUCT {
    <[(${uri})]> a ?type .
    #[ a ?type ] .
} WHERE {
    <[(${uri})]> a ?type .
    FILTER (STRSTARTS(str(?type), str(obo:))) .
}
