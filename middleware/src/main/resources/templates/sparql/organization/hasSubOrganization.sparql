PREFIX obo: <http://purl.obolibrary.org/obo/>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

CONSTRUCT {
    ?subOrganization rdfs:label ?name .
} WHERE {
    <[(${uri})]> obo:BFO_0000051 ?subOrganization .
    ?subOrganization rdfs:label ?name .
    FILTER (lang(?name) = '')
}