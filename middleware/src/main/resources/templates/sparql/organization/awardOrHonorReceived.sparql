PREFIX vivo: <http://vivoweb.org/ontology/core#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

CONSTRUCT {
    ?award rdfs:label ?title .
} WHERE {
    <[(${uri})]> vivo:relatedBy ?award .
    ?award a vivo:Award .
    ?award rdfs:label ?title .
}