PREFIX vivo: <http://vivoweb.org/ontology/core#>
PREFIX obo: <http://purl.obolibrary.org/obo/>
 
CONSTRUCT {
    ?educationalProcess vivo:majorField ?major .
} WHERE {
    <[(${uri})]> obo:RO_0000056 ?educationalProcess .
    ?educationalProcess vivo:majorField ?major .
}
